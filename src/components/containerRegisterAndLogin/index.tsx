import React from "react";
import { StyledConatainerRL } from "./style";

export const ContainerRL = ({ children }: { children: React.ReactNode }) => {
  return <StyledConatainerRL>{children}</StyledConatainerRL>;
};
